const port = 'http://localhost:3002';

 const config = {
    login: `${port}/login`,
    user: `${port}/user`,
    creatClient: `${port}/creatClient`,
    decision:  `${port}/decision`,
    decisionSearch:  `${port}/decision/search`,
}


export default config;